Read the file: problems.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. 
You have already implemented some of the fixes in FIXES.md
You have already documented the additional problems in FIXES.md that are not yet fixed
For all rows in problems.csv, see if you can change the render_json.py file to fix those changes so that only the JSON to HTML translation piece is changed. 
Ensure you extensively understand every problem and try to fix every one, and document in detail what you did in FIXES.md 
If you need to change the extract_structured_json.py, tell me what changes need to be made 
Add Python code as needed
Reference the original PDF to see what the output should look like
Take a look at the JSON and HTML to see what the results are
Continuously iterate until you resolve all of the problems or identify what needs to be changed in the extract_structured_json.py






Read the file: problems.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. 
You have already implemented some of the fixes in FIXES.md
You have already documented the additional problems in FIXES.md that are not yet fixed
For all rows in problems.csv, see if you can change the extract_structured_json.py file to fix those changes 
IMPORTANT: ENSURE YOU EXTENSIVELY EVALUATE CHANGES BEFORE DOING SO TO ENSURE YOU DO NOT INTRODUCE NEW PROBLEMS
IMPORTANT: ENSURE YOU TEST ONE FILE AT A TIME 

Ensure you extensively understand every problem and try to fix every one, and document in detail what you did in FIXES.md 
Add Python code as needed
Reference the original PDF to see what the output should look like
Take a look at the JSON and HTML to see what the results are
Continuously iterate until you resolve all of the problems or identify what needs to be changed in the extract_structured_json.py



Read the file: problems.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. 
You have already implemented some of the fixes in FIXES.md
You have already documented the additional problems in FIXES.md that are not yet fixed
For all rows in problems.csv, see if you can change the extract_structured_json.py file to fix those changes 
IMPORTANT: ENSURE YOU EXTENSIVELY EVALUATE CHANGES BEFORE DOING SO TO ENSURE YOU DO NOT INTRODUCE NEW PROBLEMS
IMPORTANT: ENSURE YOU TEST ONE FILE AT A TIME 
IMPORTANT: IF YOU MODIFY GEMINI EXTRACTION YOU MUST TEST AND MAKE SURE THESE CHANGES DIDN'T INTRODUCE NEW BUGS

Ensure you extensively understand every problem and try to fix every one, and document in detail what you did in FIXES.md 
Add Python code as needed
Reference the original PDF to see what the output should look like
Take a look at the JSON and HTML to see what the results are
Continuously iterate until you resolve all of the problems or identify what needs to be changed in the extract_structured_json.py




Read the file: problems.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. 
You have already implemented some of the fixes in FIXES.md
You have already documented the additional problems in FIXES.md that are not yet fixed
For all rows in problems.csv, see if you can change the extract_structured_json.py file to fix those changes 
IMPORTANT: ENSURE YOU EXTENSIVELY EVALUATE CHANGES BEFORE DOING SO TO ENSURE YOU DO NOT INTRODUCE NEW PROBLEMS
IMPORTANT: ENSURE YOU TEST ONE FILE AT A TIME 
IMPORTANT: IF YOU MODIFY GEMINI EXTRACTION YOU MUST TEST AND MAKE SURE THESE CHANGES DIDN'T INTRODUCE NEW BUGS

Let's work on the F. Broken/Wrong Hyperlinks (Extraction). For each link, ensure the hyperlinks are correct. 
Once you are done and have fully tested your implementation, go into FIXES.md and check off each one that is done (don't remove them - indicate that they are fixed). 
Also, address all issues in the F. Broken/Wrong Hyperlinks (Extraction) section before proceeding.



Read the file: problems.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. 
You have already implemented some of the fixes in FIXES.md
You have already documented the additional problems in FIXES.md that are not yet fixed
For all rows in problems.csv, see if you can change the extract_structured_json.py file to fix those changes 
IMPORTANT: ENSURE YOU EXTENSIVELY EVALUATE CHANGES BEFORE DOING SO TO ENSURE YOU DO NOT INTRODUCE NEW PROBLEMS
IMPORTANT: ENSURE YOU TEST ONE FILE AT A TIME 
IMPORTANT: IF YOU MODIFY GEMINI EXTRACTION YOU MUST TEST AND MAKE SURE THESE CHANGES DIDN'T INTRODUCE NEW BUGS

Let's work on these related topics
### L. Nested Tables / Table Structure Errors
### M. Multi-Line / Split Headings from Extraction
### N. Links Inside Tables Placed Below / Link Placement Issues

For each table, multiline, or related issue, ensure you implement a generalizable fix that does not introduce new problems. 
Once you are done and have fully tested your implementation, go into FIXES.md and check off each one that is done (don't remove them - indicate that they are fixed). 
Also, address all issues in the L, M, N sections before proceeding.





Read the file: critical_issues.csv - each row in that files corresponds to a PDF which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

For each item, fully understand the code and where the problems could have arise. First, is the issue still here? If it is, fix the code in render_json if possible.
Document changes in CRITICAL_ISSUES.md, or if not possible in render_json.py, indicate that here.
For all rows in critical_issues.csv, see if you can change the render_json.py file to fix those changes 
IMPORTANT: ENSURE YOU EXTENSIVELY EVALUATE CHANGES BEFORE DOING SO TO ENSURE YOU DO NOT INTRODUCE NEW PROBLEMS
IMPORTANT: ENSURE YOU TEST ONE FILE AT A TIME 
IMPORTANT: IF YOU MODIFY GEMINI EXTRACTION YOU MUST TEST AND MAKE SURE THESE CHANGES DIDN'T INTRODUCE NEW BUGS
DO NOT STOP UNTIL YOU HAVE ADDRESSED ALL ISSUES!!!


Take a look at these subdirectores inside. Here's how the files are generated.
First, the source.pdf file which underwent JSON extraction using the extract_structured_json.py script. 
After JSON extraction, that JSON underwent HTML conversion using render_json.py. 
The raw PDF files are in json_to_html_to_auditor/{file-name}/source.pdf
The generated HTML are in json_to_html_to_auditor/{file-name}/{file-name}.html
The raw JSONs are in json_to_html_to_auditor/{file-name}/{file-name}.json

Now focus on 911-education-committee-meeting-agenda-packet-03-25-2021-685f9fa9
Some of the images, such as ~/Pictures/Screenshot from 2026-03-18 18-04-10.png, do not fully show up on the screen.
Can you modify render_json.py to scale down images so that they always fully fit into the box on the HTML page that contains them?

Test your implementation on this file to ensure that all images now are contained in their boxes and fully visible.

